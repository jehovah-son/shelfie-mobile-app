import { View, Text, useColorScheme, ActivityIndicator } from "react-native";
import { Colors } from "../../constants/Colors";

export default function ThemeLoader() {
  return (
    <View>
      <Text>ThemeLoader</Text>
    </View>
  );
}
